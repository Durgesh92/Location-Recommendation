<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/preferences_cardview"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="5dp"
    android:layout_marginLeft="4dp"
    android:layout_marginRight="4dp"
    android:elevation="100dp"
    card_view:cardCornerRadius="10dp"
    card_view:cardBackgroundColor="@color/colorPrimary">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Current configuration"
            android:textColor="#ffffff"
            android:textSize="20sp"
            android:id="@+id/tv_config" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Range (Meters) :"
            android:layout_below="@+id/tv_config"
            android:paddingTop="10dp"
            android:textColor="#ffffff"
            android:textSize="15dp"
            android:id="@+id/limit_config" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=" No. of results :"
            android:layout_below="@+id/limit_config"
            android:paddingTop="10dp"
            android:textColor="#ffffff"
            android:textSize="15dp"
            android:id="@+id/result_config" />
    </RelativeLayout>

</android.support.v7.widget.CardView>